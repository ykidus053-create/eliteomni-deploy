[phases.setup]
nixPkgs = ["python3", "nodejs_20"]

[phases.install]
cmds = ["pip install -r requirements.txt --break-system-packages", "npm install -g npx"]

[start]
cmd = "python -m uvicorn app:app --host 0.0.0.0 --port $PORT --workers 1"
